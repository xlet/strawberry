package cn.w.im.core.providers.client;

import cn.w.im.core.Channel;

/**
 * no type client.
 */
public class NoTypeClient extends AbstractClient {

    public NoTypeClient(Channel channel) {
        super(channel);
    }
}
